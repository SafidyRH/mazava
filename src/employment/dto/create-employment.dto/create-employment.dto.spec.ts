import { CreateEmploymentDto } from './create-employment.dto';

describe('CreateEmploymentDto', () => {
  it('should be defined', () => {
    expect(new CreateEmploymentDto()).toBeDefined();
  });
});
